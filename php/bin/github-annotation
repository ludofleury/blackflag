#!/usr/bin/env php
<?php

$input = stream_get_contents(STDIN);

if ($input === false) {
    exit(2);
}

$count = 0;
echo str_replace('file=', 'file=php/', $input, $count);

$count > 0 ? exit(2) : exit(0);